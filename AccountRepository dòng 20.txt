AccountRepository dòng 20
  @Query("select a from Account a  where a.accountName = :accountName and a.password=:password")
    Account authenticate(@Param("accountName") String acountName, @Param("password") String password);

AccountService dòng 153
public void updateRole(Account account){
        accountRepository.save(account);
    }

UserAPIController dòng 87
 @PostMapping("/editRole")
    public ResponseEntity<?> editUserInfo(@RequestParam int userId,
                                          @RequestParam int role){
        try{
            User user = userService.getByUserId(userId);
            Account account = user.getAccount();
            account.setRoleNumber(role);
            accountService.updateRole(account);

            return new ResponseEntity<>("Đã cập nhật user", HttpStatus.OK);
        }
        catch (NoSuchElementException ex){
            return new ResponseEntity<>(HttpStatus.NO_CONTENT);
        }

    }

    fix?